
    create table tb_client (
        id bigint not null,
        birth_date timestamp,
        children integer,
        cpf varchar(255),
        income double,
        name varchar(255),
        primary key (id)
    );

    create table tb_client (
       id bigint not null,
        birth_date timestamp,
        children integer,
        cpf varchar(255),
        income double,
        name varchar(255),
        primary key (id)
    );
